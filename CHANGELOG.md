# 更新日志

## 未发布

### 仓库更名

- 仓库更名为 `chrichuang218/claude-desktop-cn`，文档链接及新版助手更新源同步调整。
- 新版仅接受新仓库地址下的更新附件，不提供旧更新源兼容；旧包直接替换为新构建。已发布的 v0.1.2 不包含此改动。
- Windows EXE 名、包名、应用 ID、安装及配置目录不随对外仓库名调整。

### 新增

- macOS 后端对齐 Windows 功能：官方 Claude 检测、安装、更新与启动，两种汉化和可验证恢复，助手三种安装方式、桌面入口、更新检查、自更新与卸载。
- macOS 以完整 `.app` 为安装、备份和替换单位，使用独立平台实现并复用现有界面、操作命令和引擎缓存。
- 平台相关安装文案与 macOS 字体适配；新增 macOS 13+ 应用包配置。
- CI 增加 Windows x64、macOS Apple Silicon 和 Intel 的测试、构建、自检与 SHA256 产物，不自动发布 Release。

### 验证边界

- Mac 双架构已在 GitHub 原生 runner 通过自动测试、生产构建、包内自检及签名/摘要校验；真实 Claude 的交互验收仍待执行。
- macOS 官方安装更新、汉化恢复、权限授权、Cowork、助手自更新和运行中卸载须按 [验收清单](docs/macos-acceptance.md) 补充实际结果。
- CI 的 macOS 包使用本地签名，未配置 Developer ID 签名和 Apple 公证；尚不承诺正式发行包的 Gatekeeper 体验。

## 0.1.2 - 2026-09-25

### 修复

- 保持 736×576 的基准比例，启动时按显示缩放和 Windows 文字大小计算窗口尺寸，避免大字体下内容被挤压。
- 窗口包含边框在内限制于当前屏幕工作区，并居中显示；不重复计算 DPI，也不持续覆盖用户手动调整的尺寸。
- 概览行不再随窗口高度撑开，保持紧凑留白。
- 设置弹窗减少留白并固定标题和关闭按钮，空间不足时仅正文滚动。

### 验证

- 缩放尺寸回归、界面多尺寸检查、构建及程序自检通过。
- 本机 Windows 122% 文字大小下比例接近基准，包含更新按钮和成功提示的设置内容完整显示；极端放大时窗口仍不超出屏幕工作区。

## 0.1.1 - 2026-09-25

### 修复

- 修复任务栏图标放大后模糊，运行中的助手改用 256px 图标。
- 便携方式默认在当前目录使用，不再额外创建子目录和复制 EXE；用户/系统安装及自选其他目录时明确说明原始文件会保留。
- 窗口按屏幕可用区域限制初始大小，支持更小窗口；修复窄窗口关闭按钮隐藏、留白过大及文字缩放下内容显示不全。

### 验证

- 构建、程序自检、便携原地安装及便携/用户安装卸载回归通过。
- 多种小窗口与 100%–200% 模拟缩放布局检查通过；本机 Windows 122% 文字大小下概览、汉化页完整显示。

## 0.1.0 - 2026-09-24

### 新增

- 首个 Windows x64 版本，提供助手安装、概览、汉化、居中设置和统一执行页。
- 接入官方 Claude Desktop 的 MSIX/Appx 检测、安装、启动与更新。
- 提供 Cowork 兼容和完整汉化；恢复要求当前包版本的有效自有备份，不删除个人数据。
- 每次补丁操作在线确认引擎修订，未变且校验有效时复用缓存；网络失败明确停止。
- 提供便携、用户和系统三种助手安装方式，并分别管理助手与 Claude Desktop 快捷方式。
- 助手自更新绑定 `chrichuang218/claude-windows-cn`，下载后校验摘要和程序自检，失败时回退。
- 提供真实操作步骤、下载进度、可复制日志与错误反馈；日志支持自动跟随和上滚暂停。
- 支持运行期间每日检查更新、高清快捷方式图标、设置键盘导航及减少动态效果。

### 验证范围与限制

- 已完成界面回归、构建和程序自检；官方包下载验签、汉化引擎缓存及隔离环境中的两种汉化/恢复往返已有验证，Cowork 兼容模式也有实机验证。
- 官方应用首次安装、原位更新及注册复核的完整成功链，系统默认安装目录的 UAC 流程、运行副本自卸载，以及助手线上自更新成功替换仍需补充端到端验证。
- 实机完整汉化后的恢复摘要、权限恢复边界及 Cowork 业务功能未完整覆盖；兼容模式不代表对所有 Claude 版本的 Cowork 功能作保证。
- 未覆盖所有 Windows 显示缩放比例，暂不支持 ARM64。
