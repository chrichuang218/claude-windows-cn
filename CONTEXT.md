# Claude 中文助手

对外仓库：`chrichuang218/claude-desktop-cn`（原 `claude-windows-cn`）。内部程序、包及配置标识沿用旧名称；改名前构建的助手需手动安装一次迁移构建。

管理 Windows 与 macOS 上的官方 Claude Desktop，并提供可选的中文补丁。两端功能范围一致；平台相关操作使用独立后端，共享界面、命令与操作状态。助手与被管理的 Claude Desktop 是两个独立产品。macOS 实机验收状态见 `docs/macos-acceptance.md`。

## 术语

**助手**：本项目提供的安装、更新、启动及汉化管理工具。
_避免_：用「Claude」同时指助手和官方应用。

**Claude Desktop**：本机安装的 Anthropic 官方桌面应用，不属于助手自身的文件和数据。

**助手安装方式**：便携方式、用户安装、系统安装三个分发选项；只决定助手的安装范围。
_避免_：Claude 便携版、用助手安装方式指代官方应用的安装方式。

**助手安装位置**：配置中的 `assistantPath` 表示目录。Windows 在目录内放置 EXE；macOS 在目录内放置完整的 `Claude 中文助手.app`，自更新以应用包为单位替换。macOS 用户安装默认为 `~/Applications`，系统安装默认为 `/Applications`。

**助手快捷方式**：打开助手界面的桌面入口，由安装页的勾选项决定。

**Claude Desktop 快捷方式**：直接打开官方 Claude Desktop 的桌面入口，由设置页的创建按钮提供。

**汉化引擎**：上游提供的语言资源与应用/恢复补丁逻辑，不是本项目的界面。

**Cowork 兼容汉化**：优先保持 Cowork 兼容性的汉化模式，在线页面的汉化范围有限。

**完整汉化**：覆盖更多在线页面、可能影响 Cowork 的汉化模式。

**恢复原样**：恢复能够确认属于当前 Claude 版本的原始应用文件，不等同于卸载助手或删除个人数据。

**执行页**：展示当前操作、真实进度和执行日志的独立页面，不是常驻诊断页。

**助手自更新**：更新本工具本身；与 Claude Desktop 的官方更新分开。
